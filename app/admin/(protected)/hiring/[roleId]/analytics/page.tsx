import { Card, CardContent } from "@/components/ui/card";

export default function RoleAnalyticsTab() {
  return (
    <Card>
      <CardContent className="text-sm text-muted-foreground">
        Pipeline funnel + response rates + rejection reasons. Coming soon.
      </CardContent>
    </Card>
  );
}
